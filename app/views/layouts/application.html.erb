<!DOCTYPE html>
<html>
<head>
  <title>Football</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>

<header class="navigation">
  <div class="navigation-wrapper">
    <a href="javascript:void(0)" class="logo">
      <%= image_tag "football-icon.png" %>
    </a>
    <a href="" class="navigation-menu-button" id="js-mobile-menu">MENU</a>
    <div class="nav">
      <ul id="navigation-menu">
        <% if signed_in? %>
          <li class="nav-link"><%= link_to "Footballer", dashboard_path %></li>
          <li class="nav-link"><%= link_to "NFL Teams", pro_teams_path %></li>
          <li class="nav-link"><%= link_to "NFL Players", players_path %></li>
        <% end %>
      </ul>
    </div>
    <div class="navigation-tools">
      <% if signed_in? %>
        <a class="sign-out"><%= button_to "Sign out", session_path, method: :delete %></a>
      <% else %>
        <a class="sign-in"><%= button_to "Sign In", new_session_path, method: :get %></a>
        <a class="sign-up"><%= button_to "Sign Up", new_user_path, method: :get %></a>
      <% end %>
    </div>
  </div>
</header>

  <div class="hero">
  <div class="hero-inner">
    <a href="" class="hero-logo"><%= image_tag "football-icon.png" %></a>
    <div class="hero-copy">
      <h1>Footballer</h1>
      <p>A place to run your fantasy football league.</p>
    </div>
  </div>
</div>
<body>
  <p>Welcome back <%= link_to current_user.username, dashboard_path %></p>
<%= yield %>

</body>
</html>
